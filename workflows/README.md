# workflows

Generated or hand-authored `.workflow` bundles can be placed here.

The default installer generates Automator workflow bundles from each feature's `feature.json` and installs them into:

```text
~/Library/Services
```
