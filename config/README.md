# config

Shared configuration files can be stored here.

The installer does not require global feature configuration. Feature behavior should live in each feature's `feature.json` whenever possible.
